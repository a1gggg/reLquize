cd ./lingquiz  //переход к папке проекта
npx expo start --tunnel запуск чтобы зайти с телефона
npx expo start -c      перезапуск
git add .
git commit -m ""
git push


- cd LingQuiz_v2
- npm run android
- npm run ios # you need to use macOS to build the iOS project - use the Expo app if you need to do iOS development without a Mac
- npm run web

//14.04
git commit -m "npx expo install expo-av // install expo av from hello-wave animated"
git commit -m "Add new logo from an old project, animated hellowave remains with errors but works"
Через ошибку  WARN  [expo-av]: Expo AV has been deprecated and will be removed in SDK 54. Use the `expo-audio` and `expo-video` packages to replace the required functionality. 
Переустанавливаю expo-av на отдельные audio and video
npx expo uninstall expo-av
npx expo install expo-audio
npx expo install expo-video
и меняю импорт на import { Audio } from "expo-audio";
исправляем все ошибки в файле hellowave
git commit -m "  We correct all errors in the files hello-wave animated"
git commit -m "  Resizing Pralax and the image inside. Compatibility tests and fixes."
//Скрываем хеадер с помощью изменения его высоты на 0. см паралакс скрол виев в компонентах
git commit -m "  We create a menu on the main page that opens a modal window."
git commit -m "  We create a menu on the main page that opens a modal window."
--------------------
npx expo doctor  проверка конфликтов
npx expo start -c      перезапуск
npx expo install --check проверить ошибки в зависимостях
npm ls  проверить ошибки в зависимостях
npm dedupe конфликты в ноде модулях
---------------------

